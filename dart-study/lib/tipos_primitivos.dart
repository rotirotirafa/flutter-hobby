String textoTipado = "String completa";
var texto = "String sem tipagem";

int numerTipado = 1;
var numero = 10;

var listaNumeros = [1, 2, 3, 4, 5];

var listaStrings = ['Carro', 'Estádio'];

// Adicionando elemento na lista de numeros
append() {
  return listaNumeros.add(10);
}

// Adicionando elemnto na lista de Strings

appendText() {
  return listaStrings.add("País");
}
